# Sender::new_with_receiver_count 文档翻译

```rust
/// 创建 [`broadcast`] 通道的发送端，并指定接收者数量。
///
/// 关于调用此函数时的更多错误信息，请参见
/// [`broadcast::channel`] 的文档。
///
/// # 安全性
///
/// 调用者必须确保在使用通道功能之前，
/// 此 Sender 的接收者数量是正确的。
/// 该计数默认为零，因为此函数本身不创建任何接收者。
```